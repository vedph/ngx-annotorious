import { OsdImgAnnotatorDirective } from './osd-img-annotator.directive';

describe('OsdImgAnnotatorDirective', () => {
  it('should create an instance', () => {
    const directive = new OsdImgAnnotatorDirective();
    expect(directive).toBeTruthy();
  });
});
